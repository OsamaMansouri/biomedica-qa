/** Contact e2e — use @example.com for predictable QA routing on api.biomedica.ma. */
export const contactGuest = {
  name: "Test Playwright",
  email: "e2e.biomedica.playwright@example.com",
  body: "Message automatique Playwright — contact form e2e.",
} as const;
